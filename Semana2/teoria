Lista de operações
<list>.append(<element>)
adiciona element como novo elemento na lista list.

+
concatena duas listas
<list> + <list>

len()
len(<list>)
tamanho de uma lista
Porem só conta elementos externos, ou seja, se houver uma lista dentro de outra lista contara 
como sendo um elemento.


for <name> in <list>:
<block>


existe a operação not in: (não esta)
if a not in b:
    block


função pop
<list>.pop() -> element
retorna o ultimo valor da list e o retira da list

